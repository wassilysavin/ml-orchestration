# Copy to .env before `docker compose up`. These credentials are shared by MinIO
# (root user) and the MLflow server / flow clients (as AWS_* for the S3 API).
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
